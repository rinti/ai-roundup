{
  "name": "ai-roundup",
  "version": "1.0.0",
  "private": true,
  "description": "Daily AI roundup site — markdown to GitHub Pages.",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "migrate": "node scripts/migrate-frontmatter.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^14.1.3"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@fontsource-variable/fraunces": "^5.2.9",
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource/ibm-plex-sans": "^5.2.8",
    "html-minifier-terser": "^7.2.0"
  }
}
